# Data

The treebank file (`treebank.csv`: sentence + gold constituency tree, one per row)
and the train/validation/test split lists will be published here after final
validation checks are completed.

Format preview:

```csv
dataset_id,sentence,parse_tree
1,<Central Kurdish sentence>,"(S (NP-SUB (...)) (VP (...)) (SENT .))"
```
